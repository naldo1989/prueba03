<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Crear Padrón</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="container">
    <h1>Crear Padrón</h1>

    <p>Escuela: <strong><%= nro_escuela %></strong></p>
    <p>Mesa: <strong><%= nro_mesa %></strong></p>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <form action="/crearPadron" method="POST">
      <input type="hidden" name="nro_escuela" value="<%= nro_escuela %>" />
      <input type="hidden" name="nro_mesa" value="<%= nro_mesa %>" />

      <label>Cantidad de votantes:</label>
      <input type="number" name="cantidad_votantes" min="1" required />

      <button type="submit">Crear y continuar</button>
    </form>

    <form action="/logout" method="POST">
      <button type="submit">Cerrar sesión</button>
    </form>
  </div>
</body>
</html>
