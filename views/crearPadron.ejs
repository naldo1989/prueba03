<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Crear Padrón</title>
  <style>
    body { font-family: Arial, sans-serif; text-align: center; padding-top: 60px; }
    input, button { margin: 6px; padding: 10px; width: 240px; }
    .error { color: red; margin-top: 10px; }
  </style>
</head>
<body>
  <h2>Crear Padrón para Escuela/Mesa</h2>
  <form action="/crearPadron" method="POST">
    <input type="text" name="nro_escuela" value="<%= nro_escuela %>" placeholder="Nro. Escuela" required /><br>
    <input type="text" name="nro_mesa" value="<%= nro_mesa %>" placeholder="Nro. Mesa" required /><br>
    <input type="number" name="cantidad_votantes" placeholder="Cantidad de Votantes" required /><br>
    <button type="submit">Guardar Padrón</button>
  </form>

  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } %>

  <p><a href="/login">Volver al login</a></p>
</body>
</html>
