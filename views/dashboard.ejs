<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Dashboard - Mesa</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="container">
    <h1>Escuela <%= nro_escuela %> - Mesa <%= nro_mesa %></h1>

    <% if (mensaje) { %>
      <div class="info"><%= mensaje %></div>
    <% } %>

    <p><strong>Total votantes:</strong> <%= cantidad_votantes %></p>
    <p><strong>Total votaron:</strong> <%= total_votaron %></p>
    <p><strong>Participación:</strong> <%= porcentaje %>%</p>

    <% if (!cerrado) { %>
      <h3>Registrar nuevos votos</h3>
      <form action="/registrar-v" method="POST">
        <input type="number" name="cantidad_votos" min="1" required />
        <button type="submit">Registrar</button>
      </form>

   <form id="cerrarForm" action="/cerrar-mesa" method="POST" onsubmit="return confirmarCierre()">
  <button type="submit">Cerrar mesa</button>
</form>

<script>
  function confirmarCierre() {
    return confirm("⚠️ ¿Estás seguro de que querés cerrar la mesa? Una vez cerrada no se podrán registrar más votos.");
  }
</script>
    <% } else { %>
      <h3 class="cerrado">Mesa Cerrada</h3>
    <% } %>

    <form action="/logout" method="POST">
      <button type="submit">Salir</button>
    </form>
  </div>
</body>
</html>
