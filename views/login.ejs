<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Login</title>
  <style>
    body { font-family: Arial, sans-serif; text-align: center; padding-top: 60px; }
    input, button { margin: 6px; padding: 10px; width: 220px; }
    .error { color: red; margin-top: 10px; }
  </style>
</head>
<body>
  <h2>Ingreso al Sistema</h2>
  <form action="/login" method="POST">
    <input type="text" name="dni" placeholder="DNI" required /><br>
    <input type="password" name="password" placeholder="Contraseña" required /><br>
    <input type="text" name="nro_escuela" placeholder="Nro. Escuela" required /><br>
    <input type="text" name="nro_mesa" placeholder="Nro. Mesa" required /><br>
    <button type="submit">Ingresar</button>
  </form>

  <% if (error) { %>
    <p class="error"><%= error %></p>
  <% } %>

  <p>¿No tenés usuario? <a href="/registro">Registrate</a></p>
</body>
</html>
