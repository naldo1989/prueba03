<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Ingreso al sistema</title>
  <style>
    body {
      background: linear-gradient(135deg, #667eea, #764ba2);
      font-family: 'Segoe UI', sans-serif;
      height: 100vh;
      margin: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      color: #333;
    }

    .login-container {
      background: #fff;
      padding: 30px 40px;
      border-radius: 16px;
      box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);
      width: 100%;
      max-width: 380px;
      text-align: center;
    }

    h2 {
      margin-bottom: 20px;
      color: #444;
    }

    label {
      display: block;
      text-align: left;
      margin-top: 12px;
      font-weight: 600;
      color: #555;
    }

    input {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 15px;
      transition: border-color 0.3s;
    }

    input:focus {
      border-color: #667eea;
      outline: none;
    }

    button {
      margin-top: 20px;
      background: #667eea;
      color: white;
      border: none;
      padding: 10px 15px;
      border-radius: 8px;
      width: 100%;
      font-size: 16px;
      cursor: pointer;
      transition: background 0.3s;
    }

    button:hover {
      background: #5563c1;
    }

    .mensaje {
      margin-top: 10px;
      font-weight: bold;
    }

    .mensaje.error {
      color: #d9534f;
    }

    .mensaje.ok {
      color: #28a745;
    }

    p {
      margin-top: 15px;
    }

    a {
      color: #667eea;
      text-decoration: none;
      font-weight: 600;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h2>Ingreso al sistema</h2>

    <% if (typeof error !== 'undefined') { %>
      <p class="mensaje error"><%= error %></p>
    <% } %>

    <% if (typeof mensaje !== 'undefined') { %>
      <p class="mensaje ok"><%= mensaje %></p>
    <% } %>

    <form action="/login" method="POST">
      <label for="dni">DNI:</label>
      <input type="text" id="dni" name="dni" required>

      <label for="password">Contraseña:</label>
      <input type="password" id="password" name="password" required>

      <label for="nro_escuela">N° de Escuela:</label>
      <input type="text" id="nro_escuela" name="nro_escuela" required>

      <label for="nro_mesa">N° de Mesa:</label>
      <input type="text" id="nro_mesa" name="nro_mesa" required>

      <button type="submit">Ingresar</button>
    </form>

    <p>¿No tienes cuenta? <a href="/registro">Registrate aquí</a></p>
  </div>
</body>
</html>
